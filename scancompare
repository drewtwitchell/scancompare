#!/bin/bash
VERSION="1.0.5"
SCRIPT_NAME="scancompare"
SCRIPT_PATH="$HOME/.local/bin/$SCRIPT_NAME"
REPO_USER="drewtwitchell"
REPO_NAME="scancompare"
REMOTE_URL="https://raw.githubusercontent.com/$REPO_USER/$REPO_NAME/main/$SCRIPT_NAME"

if [[ "$1" == "--no-update" ]]; then
  SKIP_UPDATE=true
  shift
else
  SKIP_UPDATE=false
fi

TARGET="$1"
REPORT_DIR="scan_reports"
GRYPE_OUTPUT="$REPORT_DIR/grype_output.json"
TRIVY_OUTPUT="$REPORT_DIR/trivy_output.json"
DIFF_JSON="$REPORT_DIR/diff_report.json"
HTML_REPORT="$REPORT_DIR/scan_report.html"
UPGRADED_IMAGE=""

# --- Commands ---
if [[ "$1" == "--help" || "$1" == "-h" ]]; then
  echo ""
  echo "Usage:"
  echo "  scancompare <image>      Scan and compare vulnerabilities"
  echo "  scancompare update       Manually update the CLI"
  echo "  scancompare uninstall    Remove scancompare from your system"
  echo "  scancompare --version    Show current version"
  echo ""
  exit 0
fi

if [[ "$1" == "--version" ]]; then
  echo "scancompare version $VERSION"
  exit 0
fi

if [[ "$1" == "uninstall" ]]; then
  echo "🧹 Uninstalling scancompare..."
  [[ -f "$SCRIPT_PATH" ]] && rm -f "$SCRIPT_PATH" && echo "✅ Removed $SCRIPT_PATH"
  for file in "$HOME/.bashrc" "$HOME/.zshrc" "$HOME/.profile"; do
    [[ -f "$file" ]] && sed -i.bak '/.local\/bin/d' "$file"
  done
  echo "✅ scancompare uninstalled. Restart your terminal to finish cleanup."
  exit 0
fi

# --- Auto-Update ---
get_remote_version() {
  curl -fsSL "$REMOTE_URL" | grep 'VERSION=' | cut -d'"' -f2
}

self_update() {
  tmp_file=$(mktemp)
  echo "📥 Auto-updating from $REMOTE_URL"
  curl -fsSL "$REMOTE_URL" -o "$tmp_file" || {
    echo "❌ Failed to fetch remote version."
    exit 1
  }

  remote_version=$(grep '^VERSION=' "$tmp_file" | cut -d'"' -f2)
  echo "🌐 Fetched remote version: $remote_version"

  if [[ "$remote_version" == "$VERSION" ]]; then
    rm -f "$tmp_file"
    return
  fi

  echo "🔄 New version available: $remote_version (current: $VERSION)"
  chmod +x "$tmp_file"
  mv "$tmp_file" "$SCRIPT_PATH"
  echo "✅ scancompare updated to version $remote_version"
  exec "$SCRIPT_PATH" --no-update "$@"
}

if [[ "$1" == "update" ]]; then
  self_update "$@"
  exit 0
fi

auto_update_check() {
  $SKIP_UPDATE && return
  remote_ver=$(get_remote_version)
  if [[ "$VERSION" != "$remote_ver" ]]; then
    echo "🔄 New version available: $remote_ver (current: $VERSION)"
    self_update "$@"
  fi
}
auto_update_check "$@"

# --- Tools ---
SED_BIN="sed"
if [[ "$OSTYPE" == "darwin"* ]]; then
  SED_BIN="gsed"
  command -v $SED_BIN &>/dev/null || SED_BIN="sed"
fi

check_and_install_tools() {
  for tool in grype trivy jq; do
    if ! command -v $tool &>/dev/null; then
      echo "📦 Installing $tool..."
      if command -v brew &>/dev/null; then
        brew install $tool
      else
        echo "❌ Please install $tool manually. Exiting."
        exit 1
      fi
    fi
  done
}

check_docker() {
  if ! command -v docker &>/dev/null; then
    echo "❌ Docker not installed. Install Docker Desktop: https://www.docker.com/products/docker-desktop"
    exit 1
  fi
  docker info &>/dev/null || {
    echo "⚠️ Docker is installed but not running."
    echo "👉 Please start Docker Desktop and try again."
    exit 1
  }
}

# --- Scans ---
run_scans() {
  mkdir -p "$REPORT_DIR"
  echo "🔬 Running Trivy..."
  trivy -q image --format json --output "$TRIVY_OUTPUT" "$TARGET" &
  echo "🔬 Running Grype..."
  grype "$TARGET" -o json > "$GRYPE_OUTPUT" &
  wait
}

compare_results() {
  [[ ! -s "$GRYPE_OUTPUT" || ! -s "$TRIVY_OUTPUT" ]] && echo "❌ Missing scan reports." && return 1

  grype_cves=$(jq -r '.matches[].vulnerability.id' "$GRYPE_OUTPUT" | sort | uniq)
  trivy_cves=$(jq -r '[.Results[].Vulnerabilities[]?.VulnerabilityID] | flatten | .[]' "$TRIVY_OUTPUT" | sort | uniq)
  echo "$grype_cves" > "$REPORT_DIR/grype.txt"
  echo "$trivy_cves" > "$REPORT_DIR/trivy.txt"
  only_grype=$(comm -23 "$REPORT_DIR/grype.txt" "$REPORT_DIR/trivy.txt")
  only_trivy=$(comm -13 "$REPORT_DIR/grype.txt" "$REPORT_DIR/trivy.txt")
  in_both=$(comm -12 "$REPORT_DIR/grype.txt" "$REPORT_DIR/trivy.txt")

  jq -n \
    --argjson only_in_grype "$(jq -R -s -c 'split("\n") | map(select(. != ""))' <<< "$only_grype")" \
    --argjson only_in_trivy "$(jq -R -s -c 'split("\n") | map(select(. != ""))' <<< "$only_trivy")" \
    --argjson in_both "$(jq -R -s -c 'split("\n") | map(select(. != ""))' <<< "$in_both")" '
    def count(a): a | length;
    {
      summary: {
        total_grype: (count($only_in_grype) + count($in_both)),
        total_trivy: (count($only_in_trivy) + count($in_both)),
        shared: count($in_both),
        only_in_grype: count($only_in_grype),
        only_in_trivy: count($only_in_trivy)
      },
      unique_to_grype: $only_in_grype,
      unique_to_trivy: $only_in_trivy,
      common_vulnerabilities: $in_both,
      actions: {
        suggestion: "Prioritize shared vulnerabilities and those with fixed versions."
      }
    }' > "$DIFF_JSON"
}

generate_html_report() {
  [[ ! -s "$DIFF_JSON" || ! -s "$GRYPE_OUTPUT" || ! -s "$TRIVY_OUTPUT" ]] && {
    echo "❌ Missing report data."; return 1;
  }

  {
    echo "<!DOCTYPE html><html><head><meta charset='UTF-8'><title>Scan Report</title>"
    echo "<style>body { font-family: sans-serif; padding: 20px; max-width: 900px; margin: auto; }"
    echo "h1, h2, h3 { color: #2c3e50; } pre { background: #f8f8f8; padding: 1em; border-radius: 8px; overflow-x: auto; }</style></head><body>"

    echo "<h1>Grype vs Trivy Vulnerability Report</h1>"

    jq -r '
      def cves(title; list):
        "<h3>" + title + "</h3><ul>" +
        (list | map("<li>" + . + "</li>") | join("")) +
        "</ul>";

      (.summary | "<div><strong>Total Grype:</strong> \(.total_grype) |
       <strong>Total Trivy:</strong> \(.total_trivy) |
       <strong>Shared:</strong> \(.shared)</div>") +
      cves("Only in Grype"; .unique_to_grype) +
      cves("Only in Trivy"; .unique_to_trivy) +
      cves("Common Vulnerabilities"; .common_vulnerabilities) +
      "<div><strong>Recommendation:</strong> " + .actions.suggestion + "</div>"
    ' "$DIFF_JSON"

    echo "<hr><h2>🗂 Full Trivy Output</h2><pre>"
    cat "$TRIVY_OUTPUT" | jq . || cat "$TRIVY_OUTPUT"
    echo "</pre>"

    echo "<hr><h2>🗂 Full Grype Output</h2><pre>"
    cat "$GRYPE_OUTPUT" | jq . || cat "$GRYPE_OUTPUT"
    echo "</pre>"

    echo "</body></html>"
  } > "$HTML_REPORT"
}

show_local_html_report() {
  echo "📄 Opening local HTML report..."
  if [[ -f "$HTML_REPORT" ]]; then
    case "$OSTYPE" in
      darwin*)  open "$HTML_REPORT" ;;
      linux*)   xdg-open "$HTML_REPORT" ;;
      msys*)    start "$HTML_REPORT" ;;
      cygwin*)  cygstart "$HTML_REPORT" ;;
      *)        echo "🔗 Report saved to: $HTML_REPORT" ;;
    esac
  else
    echo "❌ HTML report not found at $HTML_REPORT"
  fi
}

get_base_image() {
  grep '^FROM' Dockerfile | head -n 1 | awk '{print $2}'
}

check_for_base_image_upgrade() {
  base_image=$(get_base_image)
  [[ -z "$base_image" ]] && echo "⚠️ Could not detect base image." && return

  image_name=$(echo "$base_image" | cut -d':' -f1)
  current_tag=$(echo "$base_image" | cut -d':' -f2)
  tags=$(curl -s "https://registry.hub.docker.com/v1/repositories/${image_name}/tags" | jq -r '.[].name')
  latest=$(echo "$tags" | grep -E '^[0-9]+\.[0-9]+' | sort -V | tail -1)

  if [[ "$latest" != "$current_tag" ]]; then
    echo "⬆️  Upgrade available: $base_image → $image_name:$latest"
    $SED_BIN -i.bak "s|$base_image|$image_name:$latest|" Dockerfile
    docker build -t "${TARGET}-upgraded" .
    UPGRADED_IMAGE="${TARGET}-upgraded"
    echo "✅ Rebuilt image: $UPGRADED_IMAGE"
  else
    echo "✅ Base image is up-to-date."
  fi
}

main() {
  if [ -z "$TARGET" ]; then
    echo "❌ Usage: scancompare <docker-image>"
    exit 1
  fi

  check_docker
  check_and_install_tools

  echo "🔍 Initial scan..."
  run_scans
  compare_results
  generate_html_report
  show_local_html_report

  echo "🔧 Checking base image..."
  if [ -f Dockerfile ]; then
    check_for_base_image_upgrade
    if [[ -n "$UPGRADED_IMAGE" ]]; then
      TARGET="$UPGRADED_IMAGE"
      echo "🔁 Re-scanning upgraded image..."
      run_scans
      compare_results
      generate_html_report
      show_local_html_report
    fi
  fi
}

main "$@"
